p45 == 1 => color_sensor(0) = 5 // If pin 45 is 1, color sensor can only be mocked to return 5
p45 == 0 => color_sensor(0) = 3 // If pin 45 is 0, color sensor can only be mocked to return 3
e0 < 360 => color_sensor(1) = 0 // If the motor encoder angle is smaller than 360 then colour sensor 1 can't  see any light
